#set page(columns:2)
#title[Integration Digest]

#let df = $dif f$
#let dx = $dif x$
#let ss = $integral$
#let cc = $C$ //$C_?$

$ss_a^b f'(x) dx = f(b) - f(a) \
$

*Antidifferentiation*

$ss( df/dx )dx = f(x) + cc \
ss dx = ss 1 dx = x + cc \
$

*TODO*

- antidifferentiation patterns are reverse of differentiation
- substitution & integration by parts
- trig integration
- trig subs
  - $c - x^2 => sin$
  - $c + x^2 => tan$
  - $x^2 - c => sec$
- should trig integration even be here since its in trig digest?
